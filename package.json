{
  "name": "chainbills-admin",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "evm:register-foreign-contract": "ts-node -T src/register-foreign-contract-evm",
    "solana:initialize": "ts-node -T src/initialize-solana",
    "solana:register-foreign-contract": "ts-node -T src/register-foreign-contract-solana",
    "solana:update-max-withdrawal-fee": "ts-node -T src/update-max-withdrawal-fee-solana",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.26.0",
    "@solana/web3.js": "^1.91.7",
    "@wormhole-foundation/sdk": "^0.5.2",
    "dotenv": "^16.4.5",
    "ethers": "^6.12.1"
  }
}
