{
  "name": "chainbills-admin",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "solana:initialize": "ts-node src/initialize-solana",
    "solana:register-foreign-contract": "ts-node src/register-foreign-contract-solana",
    "test": "echo \"Error: no test specified\" && exit 1",
    "solana:update-max-withdrawal-fee": "ts-node -T src/update-max-withdrawal-fee-solana"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.26.0",
    "@solana/web3.js": "^1.91.7",
    "@wormhole-foundation/sdk": "^0.5.2",
    "dotenv": "^16.4.5"
  }
}
